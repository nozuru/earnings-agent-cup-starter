[project]
name = "eac-starter-kit"
version = "0.1.0"
description = "Earnings Agent Cup starter kit for Claude Agent SDK and Codex SDK"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "claude-agent-sdk>=0.2.122",
  "openai-codex>=0.144.4",
  "python-dotenv>=1.1.1",
]

[tool.uv]
package = false
